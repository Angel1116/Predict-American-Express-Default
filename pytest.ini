[pytest]
# Puts code/ on sys.path so tests can import the pipeline modules directly.
# This replaces what a conftest.py would otherwise do with sys.path.insert,
# and keeps each test file self-contained.
pythonpath = code
testpaths = tests
