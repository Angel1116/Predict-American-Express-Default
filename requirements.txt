# Pinned exactly, not with >=, because the differences between minor versions
# of these libraries are behavioural rather than cosmetic: LightGBM 4.x moved
# early stopping from a train() keyword into a callback, and pyarrow only
# grew usable float16 parquet support recently. A fresh machine that resolves
# to different versions will either fail loudly or, worse, score differently.
#
#     python -m pip install -r requirements.txt
#
# Tested on Python 3.12.8.

pandas==2.2.3
pyarrow==19.0.0
numpy==1.26.4
lightgbm==4.7.0
scikit-learn==1.6.1

pytest==9.1.1
